#!/bin/sh
set -eu

bun run build:css
bun run watch:css &
css_pid=$!

cleanup() {
  kill "$css_pid" 2>/dev/null || true
}

trap cleanup EXIT INT TERM

dx serve --platform web --package liroxnotes-app "$@"
