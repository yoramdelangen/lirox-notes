# Tasks

## Backlog

- Create Rust workspace layout with app, gateway, shared code, and editor package.
- Add Dioxus app shell served by Actix.
- Add Tailwind CSS and base theme tokens.
- Add mock Zed/Bear-inspired workspace shell.
- Add workspace selector in the top-left.
- Add sidebar mode switcher for file tree, label tree, and all-notes overview.
- Add bottom status bar for Git branch, sync state, changed count, and offline state.
- Add CodeMirror package with Bun/Vite build.
- Define the Dioxus-to-CodeMirror adapter API.
- Add Markdown note open/edit/save flow.
- Add changed-state tracking in file tree and notes overview.
- Add SurrealDB schema and migration runner.
- Add auth/session records and login/onboarding screens.
- Add repository and workspace records.
- Add workspace file listing API.
- Add note read/write API.
- Add note create, rename, move, and trash operations.
- Add label extraction and nested label tree.
- Add all-notes overview with compact, comfortable, and rich preview settings.
- Add search across content, filenames, display titles, labels, and frontmatter text.
- Add attachment handling for images stored beside notes and hidden from normal navigation.
- Add internal Git interface.
- Add command-based Git backend.
- Add repository connect/create flow using SSH deploy keys for hosted mode.
- Add repository cache layout and shallow clone.
- Add Git status and pending change inspection.
- Add manual sync pull/merge/commit/push flow.
- Add commit message handling with workspace default.
- Add conflict detection and conflicted file list.
- Add merge editor data format and per-block resolution.
- Add keep-all-local and keep-all-remote actions.
- Add invalid frontmatter warning.
- Add invalid config warning.
- Add broken-link highlighting.
- Add sync failure recovery state.
- Add service worker/app shell caching.
- Add IndexedDB recent note cache and pending operation queue.
- Add mobile layout pass.
- Choose default theme, default font stack, and icon set.
- Package hosted Linux build.
- Package macOS local build if hosted MVP path is stable.

## In Progress

- None.

## Done

- Planning stages 1-5.
